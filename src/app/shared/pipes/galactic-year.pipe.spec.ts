import { GalacticYearPipe } from './galactic-year.pipe';

describe('GalacticYearPipe', () => {
  it('create an instance', () => {
    const pipe = new GalacticYearPipe();
    expect(pipe).toBeTruthy();
  });
});
